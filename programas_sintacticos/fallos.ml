programa fallos;
	 i,j k;
	 a,b;
	 foo,bar
	
    escribir();
	leer;
	entacar(), entacar(true);
	escribir(, entacar(i), entacar(10), entacar(13));

	b := entacar(i);
	escribir("b=i: ", b, entacar(10), entacar(13));

	si (b = entacar(i)) ent
		escribir("correcto", entacar(10), entacar(13));
	fsi

	a := ;
	
	escribir entacar("huracan") entacar("angela");
	
	escribir("a:  ", a);

	123 := caraent(a);

	escribir("k=a: ", k, entacar(10), entacar(13));








